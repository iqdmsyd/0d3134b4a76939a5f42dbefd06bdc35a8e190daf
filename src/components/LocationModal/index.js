import React from "react";
import LocationModalView from "./LocationModalView";

const LocationModal = (props) => {
  return React.createElement(LocationModalView, { ...props });
};

export default LocationModal;
